package ${bussPackage}.service;

import com.lenovo.gsc.tech.framework.elasticsearch.service.BaseEsService;
import ${bussPackage}.entity.${className}Entity;
#if ($uniqueIndexColumns)

import java.math.BigDecimal;
import java.util.Date;
#end

/**
 * ${codeName} Service
 *
 * @author lilong10
 * @date ${dateTime}
 * Copyright 2019 Lenovo GSC Technical Service
 */
public interface ${className}Service extends BaseEsService<${className}Entity, String> {

#if ($uniqueIndexColumns)
#set($findParam = "")
#foreach($po in $!{uniqueIndexColumns})
#set($findParam = $findParam + ", " + $po.dataType + " " + $po.columnName)
#end
    /**
     * 根据唯一索引查询
     *
#foreach($po in $!{uniqueIndexColumns})
     * @param ${po.columnName} ${po.ColumnCommentLong}
#end
     * @return
     */
    ${className}Entity findByUnique($findParam.substring(2));

    /**
     * 根据唯一索引删除
     *
#foreach($po in $!{uniqueIndexColumns})
     * @param ${po.columnName} ${po.ColumnCommentLong}
#end
     * @return
     */
    Long delByUnique($findParam.substring(2));

#end
}
